@page 
@model BeansBurgers_v2.Pages.CustomizeModel 
@{ ViewData["Title"] = "Customize"; 

} 

<div>
  <h1>Beans Burgers</h1>
  <div class="dropdown">
    <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"> Cart </button>
    <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
      <ul id="cart"> @foreach (var product in Model.OrderItems) { <li>
          <table width="100%">
            <tr>
              <td width="50%"> @product.CustomBurger </td>
              <td width="25%"> $@string.Format("{0:0.00}", product.BurgerPrice) </td>
              <td width="25%"> Del btn </td>
            </tr>
          </table>
        </li> } </ul>
      <a class="btn btn-sm btn-danger order-button float-right" title="Check out" asp-page="/Menu">Checkout</a>
    </div>
  </div>
  <form method="post" asp-asp-page="/Customize"> 
    
     <div id="productsWrapper" class="row"> 
        
        @* @foreach (var ingredient in Model.Ingredients) 
        
        { 
            @if (@ingredient.Id ==1)
            {
                                <div>
                    <h1> Buns </h1>
                    <hr/>
                </div>
            }

                        @if (@ingredient.Id ==6)
            {
                                <div>
                    <h1> Patties </h1>
                    <hr/>
                </div>
            }

                                    @if (@ingredient.Id ==12)
            {
                                <div>
                    <h1> Toppings </h1>
                    <hr/>
                </div>
            }
                                    @if (@ingredient.Id ==19)
            {
                                <div>
                    <h1> Cheese </h1>
                    <hr/>
                </div>
            }
                                    @if (@ingredient.Id ==24)
            {
                                <div>
                    <h1> Sauce </h1>
                    <hr/>
                </div>
            }


            @if(@ingredient.Category == 0) { <div>
        <a title="Order @ingredient.Name">
          <div class="productInfo">
            <h3>@ingredient.Name</h3>
          </div>
        </a>
        <div class="action">
          <p class="price">$@string.Format("{0:0.00}", ingredient.ExtraCost)</p>
          <input type="radio" id="@ingredient.Id" value="@ingredient.Id" /> 
        </div>
      </div> } 
      
      @if(@ingredient.Category == 1) { <div>
        <a title="Order @ingredient.Name">
          <div class="productInfo">
            <h3>@ingredient.Name</h3> 
          </div>
        </a>
        <div class="action">
          <p class="price">$@string.Format("{0:0.00}", ingredient.ExtraCost)</p>
          <input type="radio" id="@ingredient.Id" value="@ingredient.Id" /> 
        </div>
      </div> } 
      
      @if(@ingredient.Category == 2) { <div>
        <a title="Order @ingredient.Name">
          <div class="productInfo">
            <h3>@ingredient.Name</h3> 
          </div>
        </a>


        <div class="action">
          <p class="price">$@string.Format("{0:0.00}", ingredient.ExtraCost)</p>
          <input type="checkbox" id="@ingredient.Id" value="@ingredient.Id" /> 
        </div>
      </div> } 
      
      @if(@ingredient.Category == 3) { <div>
        <a title="Order @ingredient.Name">
          <div class="productInfo">
            <h3>@ingredient.Name</h3> 
          </div>
        </a>
        <div class="action">
          <p class="price">$@string.Format("{0:0.00}", ingredient.ExtraCost)</p>
          <input type="checkbox" id="@ingredient.Id" value="@ingredient.Id" /> 
        </div>
      </div> } 
      
     }  *@
 <div>
                    <h1> Buns </h1>
                    <hr/>
                </div>

     @foreach (var buns in System.Enum.GetValues(typeof(BeansBurgers_v2.Models.BunsEnum)))
     {
        <label asp-for="Buns">@buns</label>
        <input type="radio" asp-for="Buns" value="BunsEnum"/>
     }
      <div>
                    <h1> Patties </h1>
                    <hr/>
                </div>
          @foreach (var patties in System.Enum.GetValues(typeof(BeansBurgers_v2.Models.PattiesEnum)))
     {
        <label asp-for="Patties">@patties</label>
        <input type="radio" asp-for="Patties" value="PattiesEnum"/>
     }
      <div>
                    <h1> Toppings </h1>
                    <hr/>
                </div>

          @foreach (var toppings in System.Enum.GetValues(typeof(BeansBurgers_v2.Models.ToppingsEnum)))
     {
        <label asp-for="Toppings">@toppings</label>
        <input type="radio" asp-for="Toppings" value="ToppingsEnum"/>
     }
      <div>
                    <h1> Cheese </h1>
                    <hr/>
                </div>
          @foreach (var cheese in System.Enum.GetValues(typeof(BeansBurgers_v2.Models.CheeseEnum)))
     {
        <label asp-for="Cheese">@cheese</label>
        <input type="radio" asp-for="Cheese" value="CheeseEnum"/>
     }
      <div>
                    <h1> Sauces </h1>
                    <hr/>
                </div>
          @foreach (var sauce in System.Enum.GetValues(typeof(BeansBurgers_v2.Models.SauceEnum)))
     {
        <label asp-for="Sauce">@sauce</label>
        <input type="radio" asp-for="Sauce" value="SauceEnum"/>
     }
     
     </div>

         <button class="btn btn-sm btn-danger order-button float-right" >Payment</button>

  </form>
</div> 

@section scripts{ 
    
    <partial name="_ValidationScriptsPartial"></partial>
<script type="text/javascript">
  /* Add/Subtract ingredients */
  @* $('.minus, .plus').click(function(e) {
    e.preventDefault();
    var $input = $(this).siblings('.value');
    var val = parseInt($input.val(), 10);
    if (val >= 0) {
      $input.val(val + ($(this).hasClass('minus') ? -1 : 1));
    } else {
      $input.val(val + 1);
      alert("You have no more of this ingredient");
    }
  }); *@
</script> }